# Generally it is better to set the version only for release builds, build time
# from command line using -Pversion=1.0
#
# Only use SNAPSHOT versions if you need to contribute your binaries to a
# local Maven project.
version=1.0

nb-config.org-netbeans-modules-css-prep.sass_2e_configured=true
nb-config.org-netbeans-modules-css-prep.sass_2e_enabled=true
nb-config.org-netbeans-modules-css-prep.sass_2e_mappings=/scss:/css
